#include <stdlib.h>
#include <stdio.h>
#include "menu_functions.h"

void menu(int x) {
    int option, j = 10, l = 2;
    if (x == 1) {
        printf("Press the number corresponding to the menu you want to enter\n");
    } else {
        printf("%i", x);
        exit(2);
    }
}
